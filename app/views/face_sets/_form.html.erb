<%= form_for(@face_set) do |f| %>
  <% if @face_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@face_set.errors.count, "error") %> prohibited this face_set from being saved:</h2>

      <ul>
      <% @face_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :token %><br>
    <%= f.text_field :token %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
